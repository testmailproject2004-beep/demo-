CRITICAL ISSUES TO FIX

1. EMPLOYEE PHOTOS MUST BE USER INPUT ONLY

The uploaded employee photos are the source of truth.

The LLM must receive every uploaded employee photo as image input.

STRICT RULES:

- Use the exact uploaded employee photos.
- Do not generate faces.
- Do not create avatars.
- Do not create illustrated people.
- Do not replace employees with AI-generated characters.
- Do not modify facial identity.
- Do not create lookalikes.
- The final banner must contain the actual uploaded employee photos.

Every employee photo, name, and achievement should remain associated with the same person.

------------------------------------------------

2. REFERENCE IMAGE IS NOT BEING USED PROPERLY

Currently the generated banner does not resemble the uploaded reference banner.

When a reference image is provided it should become the PRIMARY DESIGN SOURCE.

The model must analyze:

- layout structure
- color palette
- spacing
- typography style
- visual hierarchy
- employee photo placement
- decorative elements
- card designs
- overall composition

The generated output should closely follow the reference design while replacing only the content.

Reference image priority must be:

Reference Image
↓
Background Image
↓
Background Prompt

Reference image should never be ignored.

------------------------------------------------

3. ALL INPUTS MUST BE SENT TO THE LLM

Send ALL user inputs:

- title
- description
- theme
- employee names
- employee photos
- employee achievements
- reference image
- background image
- background prompt
- aspect ratio

No uploaded asset should be excluded.

------------------------------------------------

4. PHOTO + NAME + ACHIEVEMENT MUST STAY TOGETHER

Treat each employee as a single component.

Employee Card:

[Employee Photo]
[Employee Name]
[Achievement]

Rules:

- No detached names.
- No detached photos.
- No detached achievements.
- No floating labels.
- No separation between employee photo and employee information.

------------------------------------------------

5. COMPLETE BANNER MUST BE GENERATED BY THE LLM

The LLM should generate one final complete banner.

Do not:

- generate background separately
- paste employee photos later
- overlay names later
- position content with fixed coordinates

The returned image should already contain:

- background
- employee photos
- names
- achievements
- title
- theme
- decorations

as one coherent professional banner.

------------------------------------------------

6. DUPLICATE TEXT ISSUE

Rules:

- Show each employee name exactly once.
- Show each achievement exactly once.
- No duplicated labels.
- No repeated text.
- No repeated employee cards.

------------------------------------------------

7. ADD THIS TO IMAGE PROMPT

REFERENCE IMAGE PROVIDED:

This reference image is the PRIMARY DESIGN GUIDE.

Closely match:

- layout
- composition
- visual hierarchy
- color scheme
- typography style
- spacing
- employee placement
- design language

Adapt only the content and employee information.

EMPLOYEE PHOTOS PROVIDED:

The uploaded employee photos are mandatory assets.

Use the exact uploaded photos.

Do not generate new faces.
Do not generate avatars.
Do not replace the uploaded people.

Every employee photo must remain recognizable and accurately represent the uploaded person.

Generate a single complete professional banner where employee photos, names, achievements, and design elements are naturally integrated into one unified composition.


CRITICAL ISSUE

The reference image is now being respected correctly.

However, employee photos are still being modified by the image model.

Current behavior:
- Employee faces are being regenerated.
- Employee appearance is changing.
- Uploaded photos are being stylized or recreated.
- The final banner contains AI-generated versions of employees instead of the original uploaded photos.

Expected behavior:
- Employee photos must remain EXACTLY as uploaded.
- Employee photos are not reference images.
- Employee photos are not inspiration images.
- Employee photos are not face-generation inputs.

STRICT REQUIREMENT:

Reference Image:
→ Can be analyzed by the LLM for layout/style generation.

Employee Photos:
→ Must NOT be regenerated.
→ Must NOT be modified.
→ Must NOT be stylized.
→ Must NOT be recreated by AI.

The system should:

1. Generate the banner background/layout using:
   - reference image
   - theme
   - title
   - description
   - achievements

2. Preserve uploaded employee photos exactly as provided.

3. Composite the original employee photos into the generated banner without altering identity.

4. Display employee names and achievements attached to the original uploaded photo.

Prompt addition:

EMPLOYEE PHOTO PRESERVATION RULE

The uploaded employee photos are final assets.

Do not generate employee faces.
Do not recreate employee faces.
Do not modify facial features.
Do not change hairstyle, skin tone, clothing, age, gender, expression, or identity.
Do not create AI versions of employees.

These photos must appear exactly as uploaded and remain recognizable.

The only allowed operation is positioning/resizing the uploaded image within the banner layout.

The employee images are immutable source assets and must not be re-generated by the model.
