File: ContentFeedback.tsx → fetchExisting() (~line 62)
Diff
Why: content_feedback (no path param) is the POST route for submitting feedback. The GET-by-uuid route for fetching existing feedback is actually sales-feedback/{uuid} — this line was calling the wrong one, causing a 404 every time the feedback panel tried to load a previous rating.
Scope — sales only, confirmed two ways:
Every URL in this file is hardcoded to sales_platform (not domain-parameterized), so it never touches HLS or any other domain's backend.
The only current call site, ContentHistory.tsx, renders this component only when domain === 'sales'.
No other lines in the file changed — the three POST .../content_feedback calls (submit, star rating, inline comment) were already correct.
